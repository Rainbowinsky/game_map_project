# Local runtime storage

This directory is reserved for the future `LocalStorageProvider`. Runtime files are ignored by
Git; only this explanation and `.gitkeep` are committed. Do not place secrets or source assets
here.
